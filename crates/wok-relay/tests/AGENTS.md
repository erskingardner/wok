# wok-relay/tests

| File | Role |
| --- | --- |
| `protocol_prop.rs` | Client command / relay message parse properties |

Most relay behavior is covered by `crates/wok-compat/tests/` (NIP conformance, transports, plugins, error routing) and `#[cfg(test)]` modules in `src/`.
